  <div class="glide__track" data-glide-el="track">
    <ul class="slider__items glide__slides">
      <% limitProducts.forEach((limitproduct)=> { %>
      <li class="slider__item glide__slide">
        <article class="card-item">
          <a class="card-item__link" href="/shop">
            <picture class="card-item__cover">
              <span class="card-item__tag">Nuevo</span>
              <img class="card-item__img--front" src="/img<%= limitproduct.image_front %>" alt="<%= limitproduct.product_description %>">
              <img class="card-item__img--back" src="/img<%= limitproduct.image_back %>" alt="<%= limitproduct.product_description %>">
            </picture>
            <div class="card-item__content">
              <p class="card-item__licence"><%= limitproduct.licence_name %></p>
              <h4 class="card-item__name"><%= limitproduct.product_name %></h4>
              <p class="card-item__price"><%= limitproduct.price %></p>
              <p class="card-item__promo"><%= limitproduct.dues %>CUOTAS SIN INTERÉS</p>
            </div>
          </a>
        </article>
      </li>
      <% }) %>
    </ul>
  </div>
  <div class="glide__arrows" data-glide-el="controls">
    <button class="glide__arrow glide__arrow--left" data-glide-dir="<"><iconify-icon icon="material-symbols:chevron-left"></iconify-icon></button>
    <End class="glide__arrow glide__arrow--right" data-glide-dir=">"><iconify-icon icon="material-symbols:chevron-right"></iconify-icon></button>
  </div>
